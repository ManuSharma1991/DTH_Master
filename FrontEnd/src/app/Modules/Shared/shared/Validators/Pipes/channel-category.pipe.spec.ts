import { ChannelCategoryPipe } from './channel-category.pipe';

describe('ChannelCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new ChannelCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
